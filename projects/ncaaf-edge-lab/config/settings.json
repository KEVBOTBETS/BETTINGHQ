{
  "_comment": "Every knob in the model. Edit this file, commit, and the next run recalculates everything. This is the web version of the workbook's Settings sheet.",
  "season": 2026,
  "prior_season": 2025,
  "currency": "CAD",
  "currency_symbol": "C$",
  "timezone": "America/Toronto",
  "bankroll": {
    "starting": 500,
    "kelly_fraction": 0.25,
    "max_stake_pct": 0.05,
    "min_stake": 1,
    "round_stake_to": 0.5,
    "size_off": "settled",
    "_size_off_note": "'settled' = size off bankroll from settled bets only (no circularity). 'starting' = always size off the starting bankroll, like the old workbook."
  },
  "tiers": {
    "_note": "Tier input is compressed expected return per unit at the offered price, capped by no-vig model value, minus selection and confidence reserves. Probability edge is published separately. Version 2026-09-06-ev2; historical tiers stay frozen.",
    "best_bet": 0.07,
    "good": 0.045,
    "lean": 0.025
  },
  "model": {
    "projection_blend": 0.5,
    "_projection_blend_note": "Fraction of the model's disagreement with the posted line that survives into the actionable projection. College markets are softer than the NFL, so half survives, but the market remains the scale anchor.",
    "max_spread_disagreement": 12,
    "_max_spread_disagreement_note": "Raw spread disagreements are squeezed through a tanh curve before projection_blend is applied. This stops an early-season rating-scale miss from turning a 40-point favourite into a coin flip while preserving ordinary 2-6 point opinions.",
    "total_projection_blend": 0.55,
    "max_total_disagreement": 14,
    "edge_compression": 0.095,
    "_edge_compression_note": "Displayed/actionable probability edge is squeezed through 9.5% * tanh(raw/9.5%). Small edges remain useful; a bogus 20% edge is shown near the ceiling instead of presented as a real 20% advantage. Raw edge remains in the data for auditing.",
    "market_blend": 0.5,
    "_market_blend_note": "Weight given to the de-vigged market probability. Higher = more humble. The market is a consensus of everyone with money on the line; a single rating model should not assume it knows better.",
    "max_model_prob": 0.85,
    "margin_sd": 13,
    "total_sd": 10,
    "use_key_numbers": true,
    "home_field_fallback": 2.4,
    "rest_day_weight": 0.35,
    "travel_weight_per_1000mi": 0.8,
    "min_books_for_full_confidence": 2,
    "min_games_for_full_confidence": 4,
    "early_season_shrink": 0.55,
    "_early_season_shrink_note": "How hard to pull a team's current-season rating back toward its preseason prior before it has min_games_for_full_confidence games played.",
    "selection_haircut": 0.01,
    "confidence_penalty_max": 0.018,
    "_confidence_penalty_note": "Bounded expected-return reserve: (1-confidence) × 1.8%, plus the selection haircut. Confidence never multiplies the tier thresholds.",
    "_selection_haircut_note": "Winner's-curse conservatism, subtracted from every estimated edge before tiering. You only bet where the model disagrees with the market, which is also where the model's own error is largest, so selected edges are overstated even when the model is calibrated across all games -- walk-forward simulation puts that gap at 9-15 points of probability. Raise it to bet less and more selectively. Re-measure with `python -m pipeline.backtest` on your own data rather than trusting this number.",
    "_market_blend_calibration_note": "0.50 was chosen because it minimised out-of-sample calibration error in walk-forward simulation across both a sharp market and a soft one (mean absolute gap 0.045-0.050 at 0.50, vs 0.049-0.073 at 0.35 and 0.055-0.058 at 0.65). Profitability in simulation depends entirely on how inefficient you assume the market is, so it was not used to pick this value.",
    "_min_books_for_full_confidence_note": "One complete sportsbook quote is a real offer, not a consensus. Until two distinct books are observed, confidence is capped and every surviving play is LEAN-only with reduced stake."
  },
  "ratings": {
    "ridge_lambda": 18,
    "current_season_prior_strength": 6,
    "_current_season_prior_strength_note": "Bayesian weight retained on the preseason prior while current-season results accumulate. Roughly six team-games of evidence; strong enough to stabilize September, light enough for October results to take over.",
    "mov_cap": 28,
    "recency_halflife_games": 14,
    "prior_season_weight": 0.35,
    "_prior_season_weight_note": "How much last season's solved ratings carry into this season's preseason prior (regression to the mean).",
    "prior_regression": 0.62,
    "_prior_regression_note": "Fraction of last season's rating retained as the preseason prior. 0.62 ~ typical year-over-year correlation in CFB.",
    "fpi_weight": 0.85,
    "_fpi_weight_note": "Share of the preseason margin prior taken from current-season ESPN FPI. The other 15% remains this model's regressed prior-season solve, so FPI keeps its correct point scale without simply replacing the independent model.",
    "fpi_scoring_weight": 0.35,
    "_fpi_scoring_weight_note": "Lower weight for FPI's offense/defense/special-teams components in the totals model. Unit splits are more uncertain than the headline FPI rating, so the internal scoring solve retains the majority weight.",
    "scoring_prior_games": 40,
    "home_scoring_prior_games": 120,
    "_recency_note": "Half-life in team-games (x7 = days). 14 means a game from ~3 months ago counts half as much as this week's."
  },
  "markets": {
    "moneyline": true,
    "spread": true,
    "total": true
  },
  "filters": {
    "_note": "Bets that fail these are shown but never auto-logged to the ledger.",
    "preferred_min_price": -350,
    "preferred_max_price": 500,
    "_preferred_price_note": "Prices outside this preferred band may still qualify, but are capped at LEAN and staked at half size.",
    "min_price": -600,
    "max_price": 1000,
    "_hard_price_note": "Absolute pricing stop. Rows beyond this range stay visible as AVOID; they are never logged or staked.",
    "skip_if_no_odds": true,
    "max_bets_per_game": 1,
    "_max_bets_per_game_note": "Correlation guard. 1 means the model picks only its single best angle on a game (ML and ATS on the same team are ~the same bet).",
    "exclude_fcs_opponents": true,
    "soft_spread_projection_gap": 10,
    "soft_total_projection_gap": 10,
    "soft_raw_market_prob_gap": 0.22,
    "_soft_guard_note": "Crossing a soft rail caps the play at LEAN and reduces stake rather than deleting it from the actionable middle of the board.",
    "max_spread_projection_gap": 18,
    "_max_spread_projection_gap_note": "Hard spread blind-spot stop. Moderate disagreements are handled by the soft rail first.",
    "max_total_projection_gap": 18,
    "_max_total_projection_gap_note": "Hard total blind-spot stop.",
    "guard_headroom": 1.35,
    "_guard_headroom_note": "A safety rail must never sit below the bar it is protecting. Edge rises with the raw model/market gap, so a confidence-scaled tier threshold is itself a minimum raw gap in disguise; if the ceiling below lands under that minimum, the two guards overlap to zero and NOTHING can qualify at any price. That is exactly what silenced the entire 2026 opening slate. This widens the ceiling, only when it has to, to sit 35% above whatever a LEAN currently requires. Set to 1.0 to disable and apply the ceilings literally.",
    "max_raw_market_prob_gap": 0.4,
    "_max_raw_market_prob_gap_note": "Hard rejection for truly extreme disagreements before market blending can hide a ratings failure.",
    "thin_data_confidence_threshold": 0.5,
    "soft_thin_data_spread_gap": 8,
    "soft_thin_data_total_gap": 8,
    "soft_thin_data_raw_market_prob_gap": 0.18,
    "max_thin_data_spread_gap": 14,
    "max_thin_data_raw_market_prob_gap": 0.32,
    "_thin_data_guard_note": "Before both teams have meaningful current-season data, moderate gaps are LEAN-only at reduced stake; only genuinely extreme gaps are rejected.",
    "max_plays_per_week": 10,
    "_max_plays_per_week_note": "Hard cap on how many bets the model logs in a calendar week, best edges first. Without it a loose threshold plus a full Saturday slate will happily stake 60 bets."
  },
  "data": {
    "espn_group": 80,
    "_espn_group_note": "80 = FBS (I-A). Use 81 for FCS.",
    "odds_provider_priority": [
      "DraftKings",
      "ESPN BET",
      "Caesars Sportsbook",
      "consensus",
      "numberfire",
      "teamrankings"
    ],
    "lookahead_days": 10,
    "lookback_days": 5
  }
}
